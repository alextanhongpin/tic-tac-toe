# Hello Nim!
# proc main() =
#   echo "Enter your name:"
#   var name = readLine(stdin)
#   while name == "":
#     echo "What's your name?"
#     name = readLine(stdin)


# proc minimax(depth: string, maximizingPlayer: int) =
#   if depth == 0 or node.isTerminal:
#     return
#   if maximizingPlayer:
#     value = Inf
#     for each child of node do
#        value := max(value(minimax(child, depth)))
#   else:

# main()